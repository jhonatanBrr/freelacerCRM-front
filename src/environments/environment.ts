// Este archivo contiene la configuración para desarrollo (cuando trabajas localmente)
export const environment = {
  // Indica si estamos en producción (false = desarrollo)
  production: false,
  
  // URL base de tu API de Django (en desarrollo, normalmente es localhost)
  apiUrl: 'http://localhost:8000/api',
  
  // Versión de la API (útil si en el futuro necesitas hacer cambios mayores)
  apiVersion: 'v1',
};
